# Packages the queue must not build. A name here is refused as a build target
# and refused as a dependency: build-package.sh wants the deb from the
# repository instead, so anything whose dependency closure reaches an exclusion
# that is unpublished cannot build either (blocked-by-exclusion.py reports it).
# Say why, in one line — the reasons for the original list were lost with the
# machine that wrote it.
#
# 2026-09-05: at-spi2-core, sound-theme-freedesktop and libdisplay-info were
# removed from this list. They carried no recorded reason and between them
# blocked 715 of the 939 x11 recipes; they now get one honest build attempt.
angle-android
bitcoin
cabal-install
chntpw
deno
electrum
flang
foundry
gdal
geth
ghc
gpsbabel
icon-naming-utils
icoutils
lfortran
libglvnd
libgnustep-base
libseat
libsfdo
libuber-h3
lnd
mplayer
msitools
nsis
openjdk-17
openjdk-25
openxr
osm2pgsql
panda3d
predict
pypy
pypy3
ravencoin
recoll
solidity
spidermonkey
termux-gui-bash
termux-gui-c
termux-gui-package
dotnet8.0
dotnet10.0
llvm-mingw-w64
llvm-mingw-w64-tools
termux-gui-pm
virglrenderer
vlc
whitebox-tools
zeronet
dotnet9.0

# Chromium- and Gecko-class trees: each is many hours to a day on 4 vCPUs, and
# the Azure credit expires 2026-09-26. Excluded so the x11 pass spends its
# remaining time on the other 930 recipes rather than on six of them.
chromium                # chromium tree
firefox                 # gecko tree
thunderbird             # gecko tree
libreoffice             # multi-hour, pulls its own toolchain
qt5-qtwebengine         # chromium tree
qt6-qtwebengine         # chromium tree
